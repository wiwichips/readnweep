{
  "devDependencies": {
    "wrangler": "^4.65.0"
  },
  "scripts": {
    "dev": "PATH=./node_modules/.bin:$PATH uv run pywrangler dev",
    "dev:local": "PATH=./node_modules/.bin:$PATH uv run pywrangler dev --local",
    "smoke": "scripts/smoke_test.sh"
  }
}
